<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tvResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="32sp"
            android:gravity="end"
            android:padding="16dp" />

        <!-- Sayı tuşları -->
        <GridLayout
            android:id="@+id/keypad"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="3"
            android:rowCount="4"
            android:padding="8dp">

            <!-- 1-9 ve 0 için butonlar -->
            <!-- Her biri için benzer kod: -->
            <Button android:id="@+id/btn1" android:text="1" ... />
            <!-- Diğerleri de aynı şekilde btn2, btn3, ... btn0 -->

            <!-- + ve = butonları -->
            <Button android:id="@+id/btnAdd" android:text="+" ... />
            <Button android:id="@+id/btnEqual" android:text="=" ... />

            <!-- Sıfırlama butonu -->
            <Button android:id="@+id/btnClear" android:text="C" ... />
        </GridLayout>
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>

